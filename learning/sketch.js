let w = 400, h = 400;

class Clock {
    constructor(x, y, r) {
        this.x = x;
        this.y = y;
        this.r = r;
    }
}

function setup() {
  createCanvas(w, h);
}

function draw() {
  background(220);
}
